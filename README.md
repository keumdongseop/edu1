```bash
npm install
npm run init-db
npm run dev
```


```
classroom/
├── index.html     # 웹페이지 (HTML)
├── style.css      # 디자인 (CSS)
├── main.js        # 프론트엔드 (JavaScript)
├── server.js      # 백엔드 (Node.js)
└── bookings.db    # 데이터베이스 (SQLite)
```
